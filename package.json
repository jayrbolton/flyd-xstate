{
  "name": "flyd-xstate",
  "version": "0.0.2",
  "description": "unifies the functionality of xstate and flyd",
  "main": "index.js",
  "scripts": {
    "test": "dependency-check package.json && standard && node test/",
    "preversion": "npm run test",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "flyd",
    "xstate"
  ],
  "author": "jayrbolton@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.3",
    "tape": "^4.8.0"
  },
  "dependencies": {
    "flyd": "^0.2.4",
    "xstate": "^1.2.1"
  }
}
